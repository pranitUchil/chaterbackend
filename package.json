{
  "name": "chater",
  "version": "1.0.0",
  "description": "this is a mern chat app",
  "main": "app.js",
  "scripts": {
    "start": "node ./app.js",
    "build": "npm install --legacy-peer-deps && npm install --legacy-peer-deps --prefix frontend && npm run build --prefix frontend"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.1.0",
    "cookie-parser": "^1.4.6",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.0",
    "mongoose": "^7.0.4",
    "multer": "^1.4.5-lts.1",
    "socket.io": "^4.6.2"
  },
  "engines": {
    "node": ">=14.20.1"
  }
}
